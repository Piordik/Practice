#!/bin/bash
# ================================================
# recovery.sh — Восстановление базы из последнего бэкапа
# ================================================

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

"$SCRIPT_DIR/db-utils.sh" restore